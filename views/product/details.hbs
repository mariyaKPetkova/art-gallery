

 <section id="details-page">
                <h1>Details</h1>
                <article class="details-card">

                    <article class="details-card-text">
                        <h2>Title:<br>  {{product.name}}</h2>
                        <h3>Author: {{product.author.username}}</h3>
                        <h3>Painting technique: {{product.technique}}</h3>
                        <h3>Certificate of authenticity: {{product.certificate}}</h3>

                        <!-- If there is no registered user, do not display buttons-->
                        {{#if product.hasUser}}
                        <div class="buttons">
                            <!-- Only for registered user and author of the publication -->
                            {{#if product.isAuthor}}
                            <a href="/products/edit/{{product._id}}" class="btn-edit">Edit</a>
                            <a href="/products/delete/{{product._id}}" class="btn-delete">Delete</a>
                            {{/if}}
                            <!-- logged in user who has not yet shared the publication-->
                            {{#if product.isntAuthor}}
                            {{#if product.isVoted}}
                            <p class="shared-pub">You already shared this publication</p>
                            <!-- logged in user who has already shared the publication-->
                            {{else}}
                            <a href="/products/vote-up/{{product._id}}" class="btn-share">Share publication</a>
                            
                            {{/if}}
                            {{/if}}
                        </div>
                        {{/if}}

                    </article>

                    <article class="details-card-image">
                        <img src="{{product.imageUrl}}" alt="art-image2">
                    </article>

                </article>
            </section>